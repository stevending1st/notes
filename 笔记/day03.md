# day03

## 数组
```java
// 定义数组
int[] a;
int b[];

// 初始化数组
// --动态初始化，如果不赋值，系统会根据数组类型给出默认值
a = new int[10];
// --静态初始化，直接给出元素，不指定长度，给出的袁素个数就是数组长度
a= new int[]{11,22,33};  
//定义并初始化
int[] arr = {11,22,33};

// 数组根据下标赋值或取值，下标从0开始
arr[1]
```

```
int[] arr = new int[10];
```